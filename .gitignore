node_modules/
.next/
out/
*.tsbuildinfo
.DS_Store
.env*
.cache/
data/.boards.lock

# Media lives in Cloudflare R2, served from media.chele.bi.
# Kept on disk so `npm run dev` and the ingest scripts work locally, but out of
# git: 419 MB of it will not fit in a Vercel deployment, and a repo that clones
# half a gigabyte to build a 1.6 MB site is paying for nothing.
# Re-upload after adding media:  npm run media:push
public/media/
media/
